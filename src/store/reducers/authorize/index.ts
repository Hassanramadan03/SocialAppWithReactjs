import { authorizeReducer } from 'store/reducers/authorize/authorizeReducer'
import {authorizeSelector} from 'store/reducers/authorize/authorizeSelector'
export {
    authorizeReducer,
    authorizeSelector
}